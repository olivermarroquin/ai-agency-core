# Base Project Structure

This template defines the default structure for new projects.

/project-name
/docs
/src
/components
/lib
/config
/scripts

## Notes

- docs contains planning and structure
- src contains core application logic
- components contains UI elements
- lib contains shared logic
- config contains environment setup
- scripts contains utilities

This structure should be used as a starting point for all new applications.
