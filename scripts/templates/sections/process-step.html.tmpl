        <div class="{css_prefix}-process-step">
          <div class="{css_prefix}-step-badge">{step_number}</div>
          <div class="{css_prefix}-step-body">
            <h3>{title}</h3>
            <p>{body}</p>
          </div>
        </div>