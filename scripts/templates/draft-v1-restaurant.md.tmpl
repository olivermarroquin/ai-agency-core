---
type: draft
status: draft
created: {today}
updated: {today}
client: {client_slug}
page-slug: {page_slug}
target-url: /{page_slug}/
target-keyword: {focus_keyword}
business-type: restaurant
tags: [draft, page-copy, restaurant, {client_slug}]
---

# Draft — {page_title} (v1)

Auto-generated by `scaffold-page.py` on {today}.

This page uses the restaurant multi-page design system. The HTML lives in `{page_slug}.html` and the JSON-LD schema in `{page_slug}-schema.json`.

---

## Page metadata

- **URL slug:** `/{page_slug}/`
- **Page title (browser tab):** `{page_title}`
- **Meta description:** `{meta_description}`
- **Focus keyword:** `{focus_keyword}`
- **Schema type:** `Restaurant` (JSON-LD — not LocalBusiness)

---

## Page composition

- **Client:** {client_name} (data file: `data/client-{client_slug}.json`)
- **Business type:** restaurant
- **Page role:** {page_role}
- **Primary cuisine:** {primary_cuisine}
- **City:** {city_name}, {city_state}
- **Generated:** {generated_at}

## Validation checklist (before publishing)

- [ ] JSON-LD `@type` is `Restaurant` (not `LocalBusiness`)
- [ ] `servesCuisine` array populated with cuisine types
- [ ] `priceRange` matches config value
- [ ] `acceptsReservations` matches config value
- [ ] `aggregateRating` has correct `ratingValue` and `reviewCount`
- [ ] `openingHoursSpecification` covers all days from config `hours`
- [ ] If menu page: `hasMenu` block present with `@type: Menu` and `url`
- [ ] If reserve-order page: `potentialAction` blocks match config (ReserveAction if accepts_reservations, OrderAction if accepts_online_orders)
- [ ] No electrician terminology (master electrician, electrical contractor, Dominion Energy)
- [ ] No `evp-` CSS prefix (restaurant uses `rst-`)
- [ ] No `LocalBusiness` or `Service` schema types
- [ ] Owner name, phone, business name match client config
- [ ] Run Google Rich Results Test on the live URL after publishing

## Related

- [[pattern-engine-business-type-agnostic-profile-driven]]
